//карирвоание
// Задача 5: Создайте функцию filter, которая принимает массив и возвращает функцию, фильтрующую этот массив по условию, переданному как аргумент.

// Пример:

// javascript
// Копировать код
// const filter = (arr) => (predicate) => arr.filter(predicate);
// const filterEven = filter([1, 2, 3, 4, 5, 6]);
// console.log(filterEven((x) => x % 2 === 0)); // [2, 4, 6]